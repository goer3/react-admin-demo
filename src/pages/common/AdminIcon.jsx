import React from 'react';

// 下拉图标
export const AdminDropdownIcon = ({ className }) => {
  return <i className={`admin-dropdown-icon ${className}`}></i>;
};

// 收缩图标
export const AdminDropupIcon = ({ className }) => {
  return <i className={`admin-dropup-icon ${className}`}></i>;
};
